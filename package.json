{
  "name": "automation-testing-assessment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test && allure generate allure-results --clean -o allure-report",
    "test:q1": "playwright test --grep \"Q1\" && allure generate allure-results --clean -o allure-report",
    "test:q2": "playwright test --grep \"Q2\" && allure generate allure-results --clean -o allure-report",
    "test:q3": "playwright test --grep \"Q3\" && allure generate allure-results --clean -o allure-report",
    "report:html": "playwright show-report",
    "report:allure": "allure open allure-report",
    "api:test": "newman run \"Postman/ReqRes API Automation.postman_collection.json\" -e \"Postman/ReqRes Environment.postman_environment.json\" -r cli,htmlextra --reporter-htmlextra-export \"newman-report.html\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.1.2",
    "allure-commandline": "^2.43.0",
    "allure-playwright": "^3.10.2",
    "newman": "^6.2.2",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}
